services:
  - type: web
    name: jeoweb
    runtime: node
    plan: free
    buildCommand: npm install && node scan.js
    startCommand: node server.js
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
