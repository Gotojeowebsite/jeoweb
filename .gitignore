node_modules/

# Astro build outputs and cache
dist/
.astro/

# Versioned supervisor/scanner checkpoints — regenerable
pipeline_artifacts/

.venv/

# Auto-fix bot transient state — backups + per-attempt debug logs
.quarantine/
.daily-fix-last.log
scan.log
doctor.log

# Health Doctor per-run diagnostic dumps. Reports are regenerable; the
# canonical health data lives in game_health.json / scan_results.json.
reports/doctor/
reports/doctor_*.json
reports/doctor_*.txt

# Claude Code scheduled-task lockfile (per-machine)
.claude/scheduled_tasks.lock

# Per-user slug alias customizations (ship only the example)
slug_aliases.json
