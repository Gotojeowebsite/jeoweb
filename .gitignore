# Large binary game assets — stored locally only (too large for GitHub's 100 MB limit)
Assets/bendy/Build/*.unityweb
Assets/adventure-capitalist/Release/*.data

node_modules/
package-lock.json

# Versioned supervisor/scanner checkpoints — regenerable
pipeline_artifacts/

.venv/

.venv/
